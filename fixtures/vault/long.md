# Long section

Paragraph 1 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 2 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 3 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 4 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 5 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 6 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 7 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 8 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 9 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 10 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 11 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 12 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 13 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 14 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 15 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 16 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 17 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 18 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 19 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 20 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 21 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 22 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 23 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 24 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.

Paragraph 25 describes deterministic section chunking, lexical retrieval, workspace navigation, reproducible indexing, and overlapping context windows. The recurring amber-catalogue phrase lets tests inspect long-section behavior without depending on semantic search.
